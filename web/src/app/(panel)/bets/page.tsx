import BetsView from "@/components/views/bets-view";

// Data comes from the panel layout (loaded once); filters and paper/live run on it without fetching
const BetsPage = () => <BetsView />;

export default BetsPage;
