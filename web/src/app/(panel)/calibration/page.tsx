import CalibrationView from "@/components/views/calibration-view";

// Data comes from the panel layout (loaded once); paper/live switches without fetching
const CalibrationPage = () => <CalibrationView />;

export default CalibrationPage;
