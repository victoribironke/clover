import KindsView from "@/components/views/kinds-view";

// Data comes from the panel layout (loaded once); paper/live switches without fetching
const KindsPage = () => <KindsView />;

export default KindsPage;
