import OverviewView from "@/components/views/overview-view";

// Data comes from the panel layout (loaded once); the view switches paper/live without fetching
const OverviewPage = () => <OverviewView />;

export default OverviewPage;
