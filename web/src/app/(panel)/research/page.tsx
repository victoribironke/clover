import ResearchView from "@/components/views/research-view";

// Data comes from the panel layout (loaded once); the filter runs without fetching
const ResearchPage = () => <ResearchView />;

export default ResearchPage;
